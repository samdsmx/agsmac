.idea/
node_modules/
agsmac.iml
package-lock.json

# Backups creados por scripts/optimize-images.js cuando se usa --write
# (el script los deja junto a la carpeta procesada, no solo en images/)
_originals/

# Carpeta de trabajo temporal (assets en proceso, scratch, etc.)
Temp/

# --- Material de la trivia: NO se sube ------------------------------------
# Todo lo que se publica en GitHub Pages es público, y estos archivos
# contienen preguntas, respuestas y notas que arruinarían el juego si
# alguien los leyera en el repositorio. Se mantienen solo en local y se
# pegan a mano en la Google Sheet / el proyecto de Apps Script privados.
docs/apps-script-trivia.gs
docs/trivia-preguntas-sitio.md
